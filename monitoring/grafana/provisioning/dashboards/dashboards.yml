apiVersion: 1

providers:
  - name: "AI Moderation Platform"
    orgId: 1
    folder: "AI Moderation"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true

    options:
      path: /var/lib/grafana/dashboards